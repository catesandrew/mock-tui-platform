{
  "name": "mock-tui-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "start": "bun run src/cli.ts",
    "build": "bun build ./src/cli.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:e2e": "bun run build && bun test tests/e2e-smoke.test.ts"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "ink": "^5.2.1",
    "react": "^18.3.1",
    "react-devtools-core": "^4.28.5"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^18.3.12",
    "bun-types": "^1.3.1",
    "typescript": "^5.8.3"
  }
}
